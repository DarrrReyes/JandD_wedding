kind: pipeline
type: docker
name: frontend

steps:
  - name: build
    image: plugins/docker
    settings:
      tags: ${DRONE_TAG}
      dockerfile: Dockerfile
      repo: 172.20.208.1:5000/fjd/registry
      registry: 172.20.208.1:5000
      insecure: true

trigger:
  branch:
    - main
 
